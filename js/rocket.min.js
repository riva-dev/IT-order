function rocketcss(rocket, target, theclass) {

	var cloned = $(rocket).clone();
	
	var offset_target = $(target).offset();
	var offset_rocket = $(rocket).offset();
	
	//cloned.insertAfter(rocket);
	//$(rocket).css({ "opacity": "0" });
	cloned.css({ "position": "fixed", "z-index": "999999",
				 "top": offset_rocket.top + "px", "left": offset_rocket.left + "px", 
				 "right": "auto", "bottom": "auto", "margin": "auto", 
				 "padding": "auto", "opacity": "1" }).animate({ 'top': offset_target.top + 'px', 'left': offset_target.left + 'px'}, 1500);
	cloned.addClass('mover ' + (theclass || 'rocketPulseHole') );
	setTimeout(function () {
		cloned.fadeOut(300);
	}, 1500);
	return cloned;
}

$(document).ready(function() {

	/**
	 * Available animations are:
	 - rocketPulse
	 - rocketPulseHole
	 - rocketRotate
	 - rocketRotateHole
	 - rocketRotateGo
	 - rocketGost
	 - rocketFlip
	 - rocketCircle
	 */
	 
	function normalstate(norm) {
		$(norm).css({ opacity: "1" });
	}
	
	$('.btn_rocketPulse').on('click', function() {
		rocketcss(this,'.target', 'rocketPulse');
		$('.target').addClass('targetPulse');
		setTimeout(function () {
			normalstate('.rocket');
			$('.target').removeClass('targetPulse');
		}, 2300);
	});
	
	$('.btn_rocketPulseHole').on('click', function() {
		rocketcss(this,'.target', 'rocketPulseHole');
		$('.target').addClass('targetPulse');
		setTimeout(function () {
			normalstate('.rocket');
			$('.target').removeClass('targetPulse');
		}, 2300);
	});
	
	$('.btn_rocketRotate').on('click', function() {
		rocketcss(this,'.target', 'rocketRotate');
		$('.target').addClass('targetPulse');
		setTimeout(function () {
			normalstate('.rocket');
			$('.target').removeClass('targetPulse');
		}, 2300);
	});
	
	$('.btn_rocketRotateHole').on('click', function() {
		rocketcss(this,'.target', 'rocketRotateHole');
		$('.target').addClass('targetPulse');
		setTimeout(function () {
			normalstate('.rocket');
			$('.target').removeClass('targetPulse');
		}, 2300);
	});
	
	$('.btn_rocketRotateGo').on('click', function() {
		rocketcss(this,'.target', 'rocketRotateGo');
		$('.target').addClass('targetPulse');
		setTimeout(function () {
			normalstate('.rocket');
			$('.target').removeClass('targetPulse');
		}, 2300);
	});
	
	$('.btn_rocketGost').on('click', function() {
		rocketcss(this,'.target', 'rocketGost');
		$('.target').addClass('targetPulse');
		setTimeout(function () {
			normalstate('.rocket');
			$('.target').removeClass('targetPulse');
		}, 2300);
	});
	
	$('.btn_rocketFlip').on('click', function() {
		rocketcss(this,'.target', 'rocketFlip');
		$('.target').addClass('targetPulse');
		setTimeout(function () {
			normalstate('.rocket');
			$('.target').removeClass('targetPulse');
		}, 2300);
	});
	
	$('.btn_rocketCircle').on('click', function() {
		rocketcss(this,'.target', 'rocketCircle');
		$('.target').addClass('targetPulse');
		setTimeout(function () {
			normalstate('.rocket');
			$('.target').removeClass('targetPulse');
		}, 2300);
	});

});
